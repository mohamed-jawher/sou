<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .animated-gradient {
            background: linear-gradient(270deg, #1e3a8a, #d97706, #1e3a8a);
            background-size: 200% 200%;
            animation: gradientAnimation 10s ease infinite;
        }
        @keyframes gradientAnimation {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
    </style>
</head>
<body class="animated-gradient">
    <div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full bg-white/20 backdrop-blur-lg rounded-xl shadow-xl p-8 border border-white/30 text-center">
            <div class="text-red-500 text-6xl mb-4">
                <i class="fas fa-exclamation-circle"></i>
            </div>
            <h2 class="text-2xl font-bold text-white mb-4">{{message}}</h2>
            <p class="text-white/80 mb-8">يرجى طلب رابط جديد لإعادة تعيين كلمة المرور</p>
            <a href="/auth/forgot-password" 
               class="inline-block bg-yellow-500 text-white py-3 px-6 rounded-lg hover:bg-yellow-600 transition duration-300 shadow-lg font-bold">
                <i class="fas fa-redo ml-2"></i>
                طلب رابط جديد
            </a>
        </div>
    </div>
</body>
</html>